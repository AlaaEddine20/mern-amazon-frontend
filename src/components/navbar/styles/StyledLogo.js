import styled from "styled-components";

export default styled.img`
  width: 98px;
  object-fit: contain; /* logo should always look the same in every screen size (does not stretch) */
  margin-left: 20px;
  margin-right: 20px;
  margin-top: 16px;
  margin-bottom: 5px;
`;
